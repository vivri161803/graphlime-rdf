# Environments
.venv/
__pycache__/
*.pyc
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Data downloads (PyG datasets) — anchored: only the repo-root data dir
/data/

# Scratch training runs
runs/scratch/

# Notebook noise
.ipynb_checkpoints/

# Non-best checkpoints (only *_best.pt is committed)
checkpoints/*
!checkpoints/*_best.pt
!checkpoints/.gitkeep

# LaTeX build artifacts (the .tex and the built .pdf are committed)
report/*.aux
report/*.log
report/*.nav
report/*.out
report/*.snm
report/*.toc

# OS
.DS_Store
